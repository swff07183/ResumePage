import React from 'react';
import styled from 'styled-components';

interface EduItemProps {}

const EduItem = (props: EduItemProps) => {
  const {} = props;

  return <Wrapper></Wrapper>;
};

const Wrapper = styled.div``;

export default EduItem;
